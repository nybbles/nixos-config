# Twig settings template - copy to .twig/settings.toml when setting up a new project
default_source = "main"
# Note: worktrees default to ../<repo-name>-worktree/ (sibling directory)
# This is intentional - keeps worktrees separate from main repo git status
init_submodules = true  # Initialize submodules in new worktrees

# Symlinks - files shared across worktrees (Rust, Python, Go focused)
symlinks = [
  # Environment
  ".env",
  ".env.local",
  ".direnv",

  # Rust
  "target",           # Compiled artifacts

  # Python
  ".venv",            # Virtual environment
  "__pycache__",      # Bytecode cache
  ".pytest_cache",    # Test cache

  # Go
  "vendor",           # Vendored dependencies (if used)

  # Claude Code
  ".claude",          # Claude config per-project

  # Neovim
  ".bookmarks",       # bookmarks.nvim database - shared view across worktrees
]
